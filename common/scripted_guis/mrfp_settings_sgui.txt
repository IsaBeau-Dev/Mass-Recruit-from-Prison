mrfp_settings_send_option_toggle = {
  scope = character
  effect = {
    root.var:mrfp_story = {
      save_scope_as = the_story
    }
    mrfp_toggle_variable = { VARIABLE_NAME = scope:VarName }
    
  }
}