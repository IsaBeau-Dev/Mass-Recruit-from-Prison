###############################################################
# MACROS
###############################################################

# Modify a variable inside scope:the_story_owner.var:mrfp_story
mrfp_modify_story_owner_var = {
    scope:the_story_owner.var:mrfp_story = {
        change_variable = {
            name = $NAME$
            $OP$ = $VALUE$
        }
    }
}

# Send a standardized interface message
mrfp_send_message = {
    send_interface_message = {
        type = $TYPE$
        title = $TITLE$
        desc = $DESC$
        right_icon = $ICON$
    }
}

# Add ransom counters (gold/herd)
mrfp_add_ransom = {
    mrfp_modify_story_owner_var = {
        NAME = mrfp_total_ransomed_people_current
        OP = add
        VALUE = 1
    }
    mrfp_modify_story_owner_var = {
        NAME = $RESOURCE_VAR$
        OP = add
        VALUE = $AMOUNT$
    }
}

# Toggle a story variable
mrfp_toggle_story_var = {
    scope:the_story = {
        if = {
            limit = { var:$NAME$ = yes }
            set_variable = { name = $NAME$ value = no }
        }
        else = {
            set_variable = { name = $NAME$ value = yes }
        }
    }
}

###############################################################
# WAR BLOCKING
###############################################################

mrfp_is_at_war_with_target_or_their_liege_story = {
    mrfp_is_at_war_with_targets_liege_story = yes
    if = {
        limit = { is_at_war_with = scope:the_story_owner }
        add_character_flag = mrfp_blocked
    }
}

mrfp_is_at_war_with_targets_liege_story = {
    if = {
        limit = {
            liege ?= {
                is_at_war_with = scope:the_story_owner
                OR = {
                    scope:the_story_prisoner = { is_close_family_of = liege }
                    scope:the_story_prisoner = { is_primary_heir_of = liege }
                }
            }
        }
        add_character_flag = mrfp_blocked
    }
}

###############################################################
# RECRUITMENT
###############################################################

mrfp_every_prisoner_effect_story = {
    run_interaction = {
        interaction = mrfp_vars_interaction_story
        actor = scope:the_story_owner
        recipient = scope:the_story_prisoner
        execute_threshold = accept
    }
}

mrfp_update_recruitment_counter_current = {
    mrfp_modify_story_owner_var = {
        NAME = mrfp_total_recruited_current
        OP = add
        VALUE = 1
    }
}

mrfp_update_recruitment_counter = {
    change_variable = {
        name = mrfp_total_recruited
        add = scope:the_story.var:mrfp_total_recruited_current
    }
    change_variable = { name = mrfp_total_recruited_current multiply = 0 }
}

mrfp_send_amout_people_released_message = {
    if = {
        limit = { scope:the_story.var:mrfp_total_recruited_current >= 1 }
        mrfp_send_message = {
            TYPE = event_captured_combatants_good
            TITLE = prisonreleasethingy_story_release.title
            DESC = prisonreleasethingy_story_release.message
            ICON = 
        }
    }
}

mrfp_release_all_unblocked_prisoners = {
    if = { limit = { mrfp_is_unblocked_prisoner = yes } }
    mrfp_every_prisoner_effect_story = yes
}

###############################################################
# RANSOM BLOCKING
###############################################################

mrfp_block_high_value_ransoms_effect = {
    if = {
        limit = { scope:the_story_prisoner.ransom_cost_value >= 20 }
        add_character_flag = mrfp_blocked_can_ransom
    }
}

###############################################################
# RANSOM CORE
###############################################################

mrfp_ransom = {
    every_prisoner = {
        save_scope_as = the_story_prisoner
        mrfp_is_at_war_with_target_or_their_liege_story = yes
        save_scope_as = target

        if = {
            limit = { scope:the_story.var:mrfp_ransom_for_herd_enabled = no }
            mrfp_ransom_all_unblocked_prisoners_for_gold = yes
        }
        else = {
            mrfp_ransom_handle_herd_or_gold = yes
        }
    }

    mrfp_send_amount_people_ransomed_message = yes
    mrfp_update_ransom_counter = yes
}

###############################################################
# HERD/GOLD DECISION WRAPPER
###############################################################

mrfp_ransom_handle_herd_or_gold = {
    if = {
        limit = {
            scope:target = {
                government_has_flag = government_is_nomadic
                exists = domicile
            }
        }
        mrfp_ransom_all_unblocked_prisoners_for_herd = yes
    }
    else_if = {
        limit = {
            liege ?= {
                government_has_flag = government_is_nomadic
                exists = domicile
            }
        }
        liege ?= { save_scope_as = target }
        mrfp_ransom_all_unblocked_prisoners_for_herd = yes
    }
    else = {
        mrfp_ransom_all_unblocked_prisoners_for_gold = yes
    }
}

###############################################################
# GOLD RANSOM
###############################################################

mrfp_ransom_all_unblocked_prisoners_for_gold = {
	if = {
		limit = {
			mrfp_is_unblocked_prisoner = yes
		}
		scope:the_story_owner = {
			send_interface_message = {
				type = event_captured_combatants_good
				title = debug_mrfp_title
				desc = "Is unblocked prisoner check passed."
			}
		}
		
	}
	if = {
		limit = {
			mrfp_can_ransom = {
				COST = mrfp_target_ransom_gold_cost
				POOL = mrfp_secondary_recipient_gold
			}
		}
		scope:the_story_owner = {
			send_interface_message = {
				type = event_captured_combatants_good
				title = debug_mrfp_title
				desc = "Can ransom check passed."
			}
		}
	}

    if = {
        limit = {
            mrfp_is_unblocked_prisoner = yes
            mrfp_can_ransom = {
                COST = mrfp_target_ransom_gold_cost
                POOL = mrfp_secondary_recipient_gold
            }
        }

		scope:the_story_owner = {
			# mrfp_send_debug_message = yes
			
			# --- Check if target exists ---
			if = {
				limit = { NOT = { exists = scope:target } }
				send_interface_message = {
					type = event_captured_combatants_good
					title = debug_mrfp_title
					desc = "ERROR: 'target' scope does NOT exist "
				}
			}
			else = {
				send_interface_message = {
					type = event_captured_combatants_good
					title = debug_mrfp_title
					desc = "SUCCESS: 'target' scope exists"
				}
			}
		}
		

		mrfp_add_ransom = {
			RESOURCE_VAR = mrfp_total_ransomed_gold_current
			AMOUNT = mrfp_target_ransom_gold_cost
		}
	
		run_interaction = {
			interaction = ransom_interaction
			actor = scope:the_story_owner
			recipient = scope:the_story_prisoner
			execute_threshold = accept
		}
    }
	else = {
		scope:the_story_owner = {
			# --- Check if target exists ---
			if = {
				limit = { NOT = { exists = scope:target } }
				send_interface_message = {
					type = event_captured_combatants_good
					title = debug_mrfp_title
					desc = "ERROR: 'target' scope does NOT exist when checking mrfp_total_recruited_current."
				}
			}
		}
	}

}

###############################################################
# HERD RANSOM
###############################################################

mrfp_ransom_all_unblocked_prisoners_for_herd = {
    if = {
        limit = {
            mrfp_is_unblocked_prisoner = yes
            mrfp_can_ransom = {
                COST = mrfp_target_ransom_herd_cost
                POOL = mrfp_secondary_recipient_herd
            }
        }
		mrfp_add_ransom = {
			RESOURCE_VAR = mrfp_total_ransomed_herd_current
			AMOUNT = mrfp_target_ransom_herd_cost
		}
	
		scope:the_story_owner = {
			save_scope_value_as = { name = herd_send_option value = yes }
		}
	
		if = {
			limit = {
				scope:the_story_prisoner = {
					is_ruler = no
					exists = liege
				}
			}
			scope:the_story_prisoner.liege = { save_scope_as = payer }
		}
		else = {
			scope:the_story_prisoner = { save_scope_as = payer }
		}
	
		ransom_interaction_effect = yes
	
		run_interaction = {
			interaction = ransom_interaction
			actor = scope:the_story_owner
			recipient = scope:the_story_prisoner
			execute_threshold = accept
		}
    }

}

###############################################################
# RANSOM COUNTERS
###############################################################

mrfp_update_ransom_counter = {
    scope:the_story = {
        change_variable = { name = mrfp_total_ransomed_people add = scope:the_story.var:mrfp_total_ransomed_people_current }
        change_variable = { name = mrfp_total_ransomed_gold add = scope:the_story.var:mrfp_total_ransomed_gold_current }
        change_variable = { name = mrfp_total_ransomed_herd add = scope:the_story.var:mrfp_total_ransomed_herd_current }
        change_variable = { name = mrfp_total_ransomed_people_current multiply = 0 }
        change_variable = { name = mrfp_total_ransomed_gold_current multiply = 0 }
        change_variable = { name = mrfp_total_ransomed_herd_current multiply = 0 }
    }
}

###############################################################
# RANSOM MESSAGES
###############################################################

mrfp_send_amount_people_ransomed_message = {
    if = {
        limit = { scope:the_story.var:mrfp_total_ransomed_people_current >= 1 }

        if = {
            limit = {
                scope:the_story.var:mrfp_total_ransomed_herd_current >= 1
                scope:the_story.var:mrfp_total_ransomed_gold_current >= 1
            }
            mrfp_send_message = {
                TYPE = event_captured_combatants_good
                TITLE = prisonransomthingy_story.title
                DESC = prisonransomthingy_story_gold_and_herd.message
                ICON =
            }
        }
        else_if = {
            limit = { scope:the_story.var:mrfp_total_ransomed_gold_current >= 1 }
            mrfp_send_message = {
                TYPE = event_captured_combatants_good
                TITLE = prisonransomthingy_story.title
                DESC = prisonransomthingy_story_gold_only.message
                ICON =
            }
        }
        else_if = {
            limit = { scope:the_story.var:mrfp_total_ransomed_herd_current >= 1 }
            mrfp_send_message = {
                TYPE = event_captured_combatants_good
                TITLE = prisonransomthingy_story.title
                DESC = prisonransomthingy_story_herd_only.message
                ICON =
            }
        }
    }
}

###############################################################
# EVENT STORY UTILITIES
###############################################################

mrfp_toggle_variable = {
    mrfp_toggle_story_var = { NAME = $VARIABLE_NAME$ }
}

mrfp_display_warning_if_wrong_prowess_else_open = {
    if = {
        limit = { NOT = { mrfp_is_valid_prowess_threshold = yes } }
        mrfp_send_message = {
            TYPE = event_war_bad
            TITLE = mrfp_prowess_limit_error_title
            DESC = mrfp_prowess_limit_error_tooltip
            ICON = scope:actor
        }
    }
    else = { trigger_event = mrfp_set_threshold.0001 }
}

###############################################################
# STORY INITIALIZATION
###############################################################

mrfp_start_story_if_not_exist = {

    if = {
        limit = {
            NOT = { has_variable = mrfp_story }
            any_owned_story = { story_type = mrfp_recruitment_cycle }
        }
        random_owned_story = {
            limit = { story_type = mrfp_recruitment_cycle }
            save_scope_as = the_story
        }
        set_variable = { name = mrfp_story value = scope:the_story }
    }

    else_if = {
        limit = {
            has_variable = mrfp_story
            NOT = { any_owned_story = { story_type = mrfp_recruitment_cycle } }
        }
        remove_variable = mrfp_story
        create_story = mrfp_recruitment_cycle
    }

    else_if = {
        limit = {
            NOT = { has_variable = mrfp_story }
            NOT = { any_owned_story = { story_type = mrfp_recruitment_cycle } }
        }
        create_story = mrfp_recruitment_cycle
    }
}

###############################################################
# DEBUG
###############################################################

mrfp_send_debug_message = {
    mrfp_send_message = {
        TYPE = event_captured_combatants_good
        TITLE = debug_mrfp_title
        DESC = debug_mrfp_desc
        ICON =
    }
}
