mrfp_run_interaction_if_possible = {
    # scripted effect that runs a given interaction if it's potentially accepted
    # usage: mrfp_run_interaction_if_possible = { interaction = some_interaction_key }

    root= {
        send_interface_message = {
            type = event_captured_combatants_good
            title = $INTERACTION_KEY$
            desc = debug_mrfp_desc
            left_icon = root
            right_icon = this
        }
    }
    # if = {
    #     limit = {
    #         is_character_interaction_potentially_accepted = {
    #             interaction = $INTERACTION_KEY$
    #             recipient = this
    #             required_response = yes
    #         }
    #     }
    #     # run_interaction = {
    #     #     interaction = $INTERACTION_KEY$
    #     #     actor = root
    #     #     recipient = this
    #     #     execute_threshold = maybe
    #     # }
    # }
}
